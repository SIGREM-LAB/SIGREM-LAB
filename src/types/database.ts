// ARCHIVO GENERADO — no editar a mano.
//
// Se regenera con:  pnpm gen:types
// (equivale a: supabase gen types typescript --local > src/types/database.ts)
//
// Este es un placeholder vacio hasta que existan las primeras migraciones (Hito 1).
// En cuanto haya tablas, regenerarlo y commitear el resultado.

export type Database = {
  public: {
    Tables: Record<string, never>
    Views: Record<string, never>
    Functions: Record<string, never>
    Enums: Record<string, never>
    CompositeTypes: Record<string, never>
  }
}
