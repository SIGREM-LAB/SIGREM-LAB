# El cargador del formato unificado — Plan de implementacion

> **Para quien lo ejecute con agentes:** SUB-SKILL REQUERIDA: usa
> `superpowers:subagent-driven-development` (recomendada) o
> `superpowers:executing-plans` para ir tarea por tarea. Los pasos llevan
> casilla (`- [ ]`) para marcarlos.

**Objetivo:** un comando que lea los archivos del formato unificado que entregan
los almacenes, diga con precision que no puede cargar y por que, y cargue lo
demas en la base sin duplicar.

**Arquitectura:** una pasada, dos fases logicas. Se lee y se valida la hoja
entera en memoria; si tiene rechazos se escribe el CSV de excepciones y no se
abre conexion; si esta limpia, se carga con una transaccion por archivo.
`extract`, `rules` y `review` no conocen la base y se prueban sin Docker; `db`,
`catalogo` y `destino` son los unicos que hablan con Postgres.

**Stack:** Python 3.14, `openpyxl`, `psycopg[binary]`, `pytest` (nuevo).
Supabase CLI 2.105 para el entorno local. Sin migraciones: el esquema no cambia.

**Spec:** `docs/specs/2026-08-20-etl-carga-formato-unificado-design.md` — se lee
junto con este plan; el plan argumenta desde ahi.

## Restricciones globales

- **En español, sin acentos en el codigo.** Identificadores del dominio en
  español (`existencia`, `almacen`, `articulo`, `movimiento`), igual que el
  esquema. Comentarios y docstrings en español pero **sin acentos**, como el
  resto del repo. El texto que sale al usuario —mensajes del CLI y el CSV— si
  lleva acentos.
- **Sin dependencias nuevas mas alla de `pytest`.** Nada de pandas: convierte
  celdas vacias en `NaN` y la regla 6 dice que celda vacia significa **cero**.
  Nada de `supabase-py`: hace falta transaccion multi-sentencia.
- **El esquema no se toca.** Ni una migracion. El unico archivo de `supabase/`
  que cambia es `seed.sql`, que es local y no es migracion.
- **Sin `--cargar` no se escribe.** El default del CLI es simulacro.
- **El cargador no empareja por similitud.** Solo identidad exacta o alias
  exacto. Ver §6 del spec: el zinc al 95% y al 93% tienen similitud altisima y
  son dos articulos.
- **Los fixtures no se editan para que pasen las pruebas.** Si una prueba falla
  contra `etl/ejemplos/`, el error esta en el cargador. `DEFECTOS.md` es el
  oraculo.

## Estructura de archivos

| Archivo | Responsabilidad |
|---|---|
| `etl/extract/formato.py` | Lee una hoja del formato → renglones crudos por campo |
| `etl/review/informe.py` | `Problema`, `Informe`, salida a CSV |
| `etl/rules/normalizar.py` | Funciones puras: texto, numeros, NFPA, colores, enums |
| `etl/rules/validar.py` | Las 13 reglas; devuelve renglones normalizados o nada |
| `etl/cargar.py` | CLI y orquestacion |
| `etl/db.py` | Conexion, el perfil que firma, los catalogos |
| `etl/catalogo.py` | Resolver o crear articulo; alias |
| `etl/destino.py` | Escribe carga / ubicacion / existencia / movimiento |
| `etl/tests/` | pytest |

Las tareas 1 a 5 no tocan la base. Al terminar la 5 ya se pueden correr
simulacros sin Docker; las 6 a 9 agregan la escritura.

---

### Task 1: El lector del formato

**Files:**

- Create: `etl/__init__.py`, `etl/extract/__init__.py`, `etl/extract/formato.py`
- Create: `etl/tests/__init__.py`, `etl/tests/test_formato.py`
- Create: `pytest.ini`
- Modify: `etl/requirements.txt`
- Modify: `.gitignore`

**Interfaces:**

- Consumes: nada.
- Produces: `leer(ruta: Path) -> Hoja`; `Hoja` con `.almacen: str`,
  `.nombre: str`, `.responsable/.periodo/.actualizado: str | None`,
  `.filas: tuple[int, ...]`, `.renglones: tuple[dict[str, Any], ...]`.
  `CAMPOS: dict[str, dict[str, str]]` (hoja → campo → letra de columna).
  `ArchivoIlegible(Exception)`.

- [ ] **Step 1: Andamiaje**

`etl/requirements.txt` — agregar al final:

```text
pytest>=8.3
```

`pytest.ini` en la raiz de `SIGREM-LAB/`:

```ini
[pytest]
testpaths = etl/tests
python_files = test_*.py
```

`.gitignore` — agregar bajo el bloque `# Python (ETL)`:

```text
etl/review/*.csv
.pytest_cache/
```

`etl/__init__.py`, `etl/extract/__init__.py`, `etl/tests/__init__.py`: vacios.

Instalar: `pip install -r etl/requirements.txt`

- [ ] **Step 2: Escribe la prueba que falla**

`etl/tests/test_formato.py`:

```python
from pathlib import Path

import pytest

from etl.extract.formato import ArchivoIlegible, leer

LIMPIOS = Path("etl/ejemplos/datos-almacenes/limpios")
DEFECTOS = Path("etl/ejemplos/datos-almacenes/defectos")


def test_saca_el_almacen_y_la_hoja_del_nombre_del_archivo():
    hoja = leer(LIMPIOS / "N3" / "N3-Reactivos.xlsx")
    assert hoja.almacen == "N3"
    assert hoja.nombre == "Reactivos"


def test_lee_los_14_renglones_de_n3_reactivos():
    hoja = leer(LIMPIOS / "N3" / "N3-Reactivos.xlsx")
    assert len(hoja.renglones) == 14


def test_reactivos_arranca_en_la_fila_10_y_las_demas_en_la_9():
    # Reactivos lleva tres filas de encabezado agrupado por los apartados
    # de la NOM-005-STPS; las otras cinco hojas, una.
    assert leer(LIMPIOS / "N3" / "N3-Reactivos.xlsx").filas[0] == 10
    assert leer(LIMPIOS / "N3" / "N3-Equipos.xlsx").filas[0] == 9


def test_la_columna_anaquel_de_reactivos_cae_en_mueble():
    # El formato la titula "Anaquel", pero el esquema unifico a `mueble`:
    # "Anaquel 2" es un VALOR de mueble, igual que "Gabinete 301".
    primero = leer(LIMPIOS / "N3" / "N3-Reactivos.xlsx").renglones[0]
    assert primero["mueble"] == "Anaquel 2"
    assert "anaquel" not in primero


def test_lee_el_encabezado_que_alimenta_la_tabla_carga():
    hoja = leer(LIMPIOS / "N3" / "N3-Equipos.xlsx")
    assert hoja.responsable == "Q.A. Laura Mendoza Vega"
    assert hoja.periodo == "Enero-Julio 2026"
    assert hoja.actualizado == "19/08/2026"


def test_ignora_las_hojas_de_texto():
    # Los 15 archivos llevan "Reglas de captura" y "Léeme"; ninguna es datos.
    assert leer(LIMPIOS / "LE" / "LE-Electronica.xlsx").nombre == "Electrónica"


def test_los_ejemplos_sin_borrar_se_leen_como_renglones():
    # Defecto 22: la cursiva no es un dato. El lector los entrega y quien
    # decide que hacer con ellos es la validacion.
    assert len(leer(DEFECTOS / "N4" / "N4-Material.xlsx").renglones) == 13


def test_rechaza_un_archivo_cuya_hoja_no_coincide_con_su_nombre(tmp_path):
    copia = tmp_path / "N3-Insumos.xlsx"
    copia.write_bytes((LIMPIOS / "N3" / "N3-Equipos.xlsx").read_bytes())
    with pytest.raises(ArchivoIlegible, match="Equipos"):
        leer(copia)
```

- [ ] **Step 3: Correr y ver que falla**

Run: `python -m pytest etl/tests/test_formato.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'etl.extract.formato'`

- [ ] **Step 4: Implementar**

`etl/extract/formato.py`:

```python
"""Lee una hoja del formato unificado.

El almacen sale del NOMBRE del archivo (N3-Equipos.xlsx), no de una columna:
decision D1 del spec, Nivel 1 del Leeme del formato. La hoja se toma del libro
—hay exactamente una de datos— y se coteja contra el nombre del archivo, para
que un archivo mal rotulado no pase inadvertido.
"""

from __future__ import annotations

import unicodedata
from dataclasses import dataclass
from pathlib import Path
from typing import Any

from openpyxl import load_workbook

# Reactivos lleva tres filas de encabezado agrupado (los apartados de la
# NOM-005-STPS), asi que sus titulos de columna van una fila mas abajo.
FILA_ENCABEZADO = {"Reactivos": 9}
FILA_ENCABEZADO_DEFECTO = 8

HOJAS_DE_DATOS = ("Reactivos", "Insumos", "Material", "Equipos",
                  "Materia biológica", "Electrónica")

# Que columna del formato es que campo. Lo que no esta aqui se ignora: "No." es
# el consecutivo del Excel, y "7.1-a) Uso principal", "Zona de riesgo" y
# "Numero de personas expuestas" son constantes por almacen que viven en la
# tabla `almacen` (decision D4 del spec del 18 de agosto).
CAMPOS: dict[str, dict[str, str]] = {
    "Reactivos": {
        # Ojo con C: el formato la titula "Anaquel", el esquema unifico a mueble.
        "sub_ubicacion": "B", "mueble": "C", "repisa": "D", "fila_cajon": "E",
        "color": "F", "hoja_seguridad": "G", "sustancia": "H", "marca": "I",
        "presentacion": "J", "peso_vacio": "K", "peso_total": "L",
        "cantidad": "M", "unidad": "N",
        "solido": "P", "liquido": "Q", "gas": "R",
        "caracteristica_quimica": "S", "caracteristica_toxica": "T",
        "riesgo_salud": "U", "riesgo_reactividad": "V",
        "riesgo_inflamabilidad": "W", "peligro_especial": "X",
        "implica_peligro": "Y", "observaciones": "AB",
    },
    "Insumos": {
        "clasificacion": "B", "articulo": "C", "especificacion": "D",
        "marca": "E", "cantidad": "F", "unidad": "G", "presentacion": "H",
        "sub_ubicacion": "I", "mueble": "J", "repisa": "K", "fila_cajon": "L",
        "observaciones": "M",
    },
    "Equipos": {
        "articulo": "B", "marca": "C", "modelo": "D", "numero_serie": "E",
        "numero_inventario": "F", "sub_ubicacion": "G", "laboratorio": "H",
        "mueble": "I", "funcionamiento": "J", "fecha_chequeo": "K",
        "mantenimiento": "L", "observaciones": "M",
    },
    "Materia biológica": {
        "articulo": "B", "origen_especie": "C", "cantidad": "D", "unidad": "E",
        "presentacion": "F", "metodo_conservacion": "G", "temperatura": "H",
        "fecha_recoleccion": "I", "fecha_preparacion": "J",
        "responsable_muestra": "K", "sub_ubicacion": "L", "mueble": "M",
        "repisa": "N", "observaciones": "O",
    },
    "Electrónica": {
        "clasificacion": "B", "familia": "C", "articulo": "D",
        "especificacion": "E", "cantidad": "F", "unidad": "G",
        "presentacion": "H", "sub_ubicacion": "I", "mueble": "J",
        "coord_h": "K", "coord_v": "L", "coord_i": "M", "observaciones": "N",
    },
}
# Las dos hojas son identicas entre si.
CAMPOS["Material"] = CAMPOS["Insumos"]


class ArchivoIlegible(Exception):
    """El archivo no es una hoja del formato unificado, o no la que dice ser."""


@dataclass(frozen=True)
class Hoja:
    ruta: Path
    almacen: str
    nombre: str
    responsable: str | None
    periodo: str | None
    actualizado: str | None
    filas: tuple[int, ...]
    renglones: tuple[dict[str, Any], ...]

    @property
    def archivo(self) -> str:
        return self.ruta.name


def slug(nombre: str) -> str:
    """«Materia biológica» → «materia-biologica». Publica: la usa cargar.py."""
    d = unicodedata.normalize("NFD", nombre.lower())
    return "".join(c for c in d if unicodedata.category(c) != "Mn").replace(" ", "-")


def leer(ruta: Path) -> Hoja:
    almacen, _, resto = ruta.stem.partition("-")
    if not almacen or not resto:
        raise ArchivoIlegible(
            f"{ruta.name}: el nombre debe ser ALMACEN-Hoja.xlsx")

    libro = load_workbook(ruta, data_only=True)
    candidatas = [h for h in libro.sheetnames if h in HOJAS_DE_DATOS]
    if len(candidatas) != 1:
        raise ArchivoIlegible(
            f"{ruta.name}: se esperaba una hoja de datos y hay "
            f"{len(candidatas)}: {candidatas}")

    nombre = candidatas[0]
    if slug(nombre) != resto.lower():
        raise ArchivoIlegible(
            f"{ruta.name}: el archivo dice «{resto}» y la hoja es «{nombre}»")

    ws = libro[nombre]
    campos = CAMPOS[nombre]
    fila_enc = FILA_ENCABEZADO.get(nombre, FILA_ENCABEZADO_DEFECTO)

    filas: list[int] = []
    renglones: list[dict[str, Any]] = []
    for r in range(fila_enc + 1, ws.max_row + 1):
        crudo = {campo: ws[f"{letra}{r}"].value for campo, letra in campos.items()}
        if all(v is None for v in crudo.values()):
            continue
        filas.append(r)
        renglones.append(crudo)

    return Hoja(
        ruta=ruta, almacen=almacen, nombre=nombre,
        responsable=ws["F4"].value, periodo=ws["B5"].value,
        actualizado=ws["F5"].value,
        filas=tuple(filas), renglones=tuple(renglones),
    )
```

- [ ] **Step 5: Correr y ver que pasa**

Run: `python -m pytest etl/tests/test_formato.py -v`
Expected: PASS, 8 pruebas.

- [ ] **Step 6: Commit**

```bash
git add etl/__init__.py etl/extract/ etl/tests/ pytest.ini etl/requirements.txt .gitignore
git commit -m "feat(etl): lector del formato unificado"
```

---

### Task 2: El informe de excepciones

**Files:**

- Create: `etl/review/__init__.py`, `etl/review/informe.py`
- Create: `etl/tests/test_informe.py`

**Interfaces:**

- Consumes: nada.
- Produces: `Problema` (dataclass frozen: `archivo, hoja, fila, columna, regla,
  valor, accion, detalle`), `Informe` con `.anotar(...)`, `.problemas`,
  `.rechazos`, `.normalizados`, `.archivos_rechazados`, `.a_csv(ruta) -> Path`.
  `Accion = Literal["rechazo", "normalizado"]`.

- [ ] **Step 1: Escribe la prueba que falla**

`etl/tests/test_informe.py`:

```python
import csv

from etl.review.informe import Informe, Problema


def _p(**kw) -> Problema:
    base = dict(archivo="N3-Reactivos.xlsx", hoja="Reactivos", fila=12,
                columna="M", regla="Regla 1", valor="Un frasco",
                accion="rechazo", detalle="no es un número")
    return Problema(**{**base, **kw})


def test_separa_rechazos_de_normalizados():
    inf = Informe()
    inf.anotar(_p())
    inf.anotar(_p(accion="normalizado", columna="J", valor="Husky "))
    assert len(inf.rechazos) == 1
    assert len(inf.normalizados) == 1
    assert len(inf.problemas) == 2


def test_dice_que_archivos_quedaron_rechazados():
    inf = Informe()
    inf.anotar(_p())
    inf.anotar(_p(archivo="N3-Insumos.xlsx", accion="normalizado"))
    # Un normalizado NO tumba el archivo; un rechazo si.
    assert inf.archivos_rechazados == {"N3-Reactivos.xlsx"}


def test_escribe_un_csv_que_excel_abre_bien(tmp_path):
    inf = Informe()
    inf.anotar(_p(detalle="acentuación y «comillas»"))
    ruta = inf.a_csv(tmp_path / "informe.csv")

    with ruta.open(encoding="utf-8-sig", newline="") as f:
        filas = list(csv.DictReader(f))

    assert filas[0]["archivo"] == "N3-Reactivos.xlsx"
    assert filas[0]["fila"] == "12"
    assert filas[0]["accion"] == "rechazo"
    assert filas[0]["detalle"] == "acentuación y «comillas»"


def test_un_informe_vacio_igual_escribe_el_encabezado(tmp_path):
    ruta = Informe().a_csv(tmp_path / "vacio.csv")
    assert ruta.read_text(encoding="utf-8-sig").splitlines()[0].startswith("archivo")
```

- [ ] **Step 2: Correr y ver que falla**

Run: `python -m pytest etl/tests/test_informe.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'etl.review.informe'`

- [ ] **Step 3: Implementar**

`etl/review/informe.py`:

```python
"""El CSV de excepciones: que no se pudo cargar, y que se corrigio solo.

Dos clases de problema, y la diferencia decide si la hoja entra o no:

  rechazo      el renglon viola una regla y no se puede adivinar la intencion.
               La hoja NO se carga.
  normalizado  el ETL sabe que hacer sin preguntar. La hoja SI se carga.

La columna `accion` es comparable renglon a renglon con la columna «Espera» de
etl/ejemplos/DEFECTOS.md, que es lo que hace posible la prueba de aceptacion.
"""

from __future__ import annotations

import csv
from dataclasses import asdict, dataclass
from pathlib import Path
from typing import Any, Literal

Accion = Literal["rechazo", "normalizado"]

COLUMNAS = ("archivo", "hoja", "fila", "columna", "regla", "valor",
            "accion", "detalle")


@dataclass(frozen=True)
class Problema:
    archivo: str
    hoja: str
    fila: int | None      # fila de Excel, para poder ir a verla. None = toda la hoja
    columna: str          # letra de columna, o "" si el problema no es de una celda
    regla: str
    valor: Any
    accion: Accion
    detalle: str


class Informe:
    def __init__(self) -> None:
        self._problemas: list[Problema] = []

    def anotar(self, problema: Problema) -> None:
        self._problemas.append(problema)

    @property
    def problemas(self) -> list[Problema]:
        return list(self._problemas)

    @property
    def rechazos(self) -> list[Problema]:
        return [p for p in self._problemas if p.accion == "rechazo"]

    @property
    def normalizados(self) -> list[Problema]:
        return [p for p in self._problemas if p.accion == "normalizado"]

    @property
    def archivos_rechazados(self) -> set[str]:
        return {p.archivo for p in self.rechazos}

    def a_csv(self, ruta: Path) -> Path:
        ruta.parent.mkdir(parents=True, exist_ok=True)
        # utf-8-sig: sin el BOM, Excel abre el CSV en la codificacion del
        # sistema y los acentos salen rotos. Este archivo lo va a abrir gente.
        with ruta.open("w", encoding="utf-8-sig", newline="") as f:
            escritor = csv.DictWriter(f, fieldnames=COLUMNAS)
            escritor.writeheader()
            for p in self._problemas:
                escritor.writerow(asdict(p))
        return ruta

    def __len__(self) -> int:
        return len(self._problemas)
```

`etl/review/__init__.py`: vacio.

- [ ] **Step 4: Correr y ver que pasa**

Run: `python -m pytest etl/tests/test_informe.py -v`
Expected: PASS, 4 pruebas.

- [ ] **Step 5: Commit**

```bash
git add etl/review/ etl/tests/test_informe.py
git commit -m "feat(etl): informe de excepciones a CSV"
```

---

### Task 3: Las normalizaciones

**Files:**

- Create: `etl/rules/__init__.py`, `etl/rules/normalizar.py`
- Create: `etl/tests/test_normalizar.py`

**Interfaces:**

- Consumes: nada.
- Produces: `Valor` (dataclass frozen: `.dato: Any`, `.aviso: str | None`),
  `Rechazo(Exception)` con `.regla` y `.detalle`, y las funciones puras
  `clave(str) -> str`, `texto`, `numero`, `numero_opcional`,
  `numero_inventario`, `grado_nfpa`, `color`, `si_no`, `estado_fisico`,
  `funcionamiento`. Todas devuelven `Valor` o lanzan `Rechazo`.

- [ ] **Step 1: Escribe la prueba que falla**

`etl/tests/test_normalizar.py`:

```python
import pytest

from etl.rules.normalizar import (
    Rechazo, clave, color, estado_fisico, funcionamiento, grado_nfpa, numero,
    numero_inventario, numero_opcional, si_no, texto,
)


# Regla 3
def test_texto_colapsa_espacios_y_avisa():
    v = texto("Gabinete  309")
    assert v.dato == "Gabinete 309"
    assert v.aviso is not None


def test_texto_recorta_el_espacio_final():
    assert texto("Husky ").dato == "Husky"


def test_texto_limpio_no_genera_aviso():
    assert texto("Husky").aviso is None


# Contrato ETL §3
@pytest.mark.parametrize("crudo", ["—", "S/N", "Sin serie", "Sin inventario",
                                   "Sin modelo", "", "   "])
def test_las_formas_de_ausencia_se_leen_como_nulo(crudo):
    assert texto(crudo).dato is None


# Regla 1 y regla 6
def test_numero_acepta_numeros():
    assert numero(139.8).dato == 139.8


def test_celda_vacia_es_cero():
    assert numero(None).dato == 0


@pytest.mark.parametrize("crudo", ["Un frasco", "2 (1 con 5 menos)", "*", "kit"])
def test_texto_donde_va_un_numero_se_rechaza(crudo):
    with pytest.raises(Rechazo):
        numero(crudo)


def test_los_pesos_vacios_son_nulos_no_cero():
    # N4 captura la cantidad directa y deja los dos pesos vacios; un cero ahi
    # significaria "el frasco pesa 0 g".
    assert numero_opcional(None).dato is None
    assert numero_opcional("—").dato is None


# Regla 10
def test_quita_los_ceros_a_la_izquierda_del_inventario():
    v = numero_inventario("05311300206")
    assert v.dato == "5311300206"
    assert v.aviso is not None


# NFPA
def test_grado_nfpa_de_texto_a_numero():
    assert grado_nfpa("Grado 2: Riesgo moderado").dato == 2
    assert grado_nfpa("Grado 0: Riesgo mínimo").dato == 0


def test_grado_nfpa_desconocido_se_rechaza():
    with pytest.raises(Rechazo):
        grado_nfpa("Muy peligroso")


# Regla 12
def test_los_seis_colores_pasan():
    assert color("Verde").dato == "verde"
    assert color("Naranja").dato == "naranja"


def test_no_toxico_se_normaliza_a_verde_con_aviso():
    v = color("NO TÓXICO")
    assert v.dato == "verde"
    assert v.aviso is not None


def test_un_color_que_no_existe_se_rechaza():
    with pytest.raises(Rechazo):
        color("Morado")


def test_color_vacio_se_rechaza():
    # Defecto 13: la celda esta pintada de rojo pero vacia. El ETL lee
    # valores, no formato.
    with pytest.raises(Rechazo):
        color(None)


# Regla 11
def test_un_solo_estado_fisico():
    assert estado_fisico("x", None, None).dato == "solido"
    assert estado_fisico(None, "x", None).dato == "liquido"


def test_dos_estados_fisicos_se_rechaza():
    with pytest.raises(Rechazo, match="2"):
        estado_fisico("x", "x", None)


def test_ningun_estado_fisico_se_rechaza():
    with pytest.raises(Rechazo):
        estado_fisico(None, None, None)


# Enum funcionamiento_equipo
def test_los_dos_valores_del_enum_pasan():
    assert funcionamiento("Correcto").dato == "correcto"
    assert funcionamiento("Presenta fallas").dato == "presenta_fallas"


def test_no_funciona_se_rechaza():
    # El desplegable del formato lo ofrece; el enum no lo tiene.
    with pytest.raises(Rechazo, match="enum"):
        funcionamiento("No funciona")


def test_si_no_a_booleano():
    assert si_no("Sí").dato is True
    assert si_no("No").dato is False


def test_clave_es_lo_que_hace_norm_texto_en_la_base():
    # Sin esto el lookup de laboratorio falla: la base los tiene sin acentos.
    assert clave("Caracterización y procesamiento") == "caracterizacion y procesamiento"
```

- [ ] **Step 2: Correr y ver que falla**

Run: `python -m pytest etl/tests/test_normalizar.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'etl.rules.normalizar'`

- [ ] **Step 3: Implementar**

`etl/rules/normalizar.py`:

```python
"""Funciones puras que convierten una celda del Excel en un valor del esquema.

Cada una devuelve un `Valor` —con un aviso si tuvo que corregir algo— o lanza
`Rechazo` si el dato no se puede salvar sin adivinar. Ninguna toca la base ni
sabe de que hoja viene la celda: por eso se prueban en milisegundos.
"""

from __future__ import annotations

import unicodedata
from dataclasses import dataclass
from typing import Any

# Contrato ETL §3. Es lo que hace que los unique parciales de serie y de
# inventario funcionen: hacen falta tantos NULL como equipos sin identificar.
VACIOS = frozenset({"", "-", "—", "s/n", "n/a", "sin serie", "sin modelo",
                    "sin inventario", "sin marca"})

GRADOS = {"grado 0": 0, "grado 1": 1, "grado 2": 2, "grado 3": 3, "grado 4": 4}

COLORES = frozenset({"verde", "rojo", "azul", "blanco", "amarillo", "naranja"})
# Regla 12: "NO TOXICO" no es un color. 143 renglones de N3 lo dicen.
SINONIMOS_COLOR = {"no toxico": "verde"}

FUNCIONAMIENTO = {"correcto": "correcto", "presenta fallas": "presenta_fallas"}

R_NUMERO = "Regla 1 · en una columna de números va solo el número"
R_ESPACIOS = "Regla 3 · sin espacios de sobra ni dobles"
R_VACIO_CERO = "Regla 6 · celda vacía significa cero"
R_INVENTARIO = "Regla 10 · el número de inventario no se repite entre renglones"
R_COLOR = "Regla 12 · los colores de almacenaje son seis"
R_ESTADO = "Regla 11 · un solo estado físico por reactivo"
R_ENUM_FUNC = "Choque con el esquema · enum funcionamiento_equipo"


class Rechazo(Exception):
    def __init__(self, regla: str, detalle: str) -> None:
        super().__init__(detalle)
        self.regla = regla
        self.detalle = detalle


@dataclass(frozen=True)
class Valor:
    dato: Any
    aviso: str | None = None
    # Que regla justifica el aviso. Sin esto, quien anota en el informe tendria
    # que adivinarla del texto del aviso, y la prueba de aceptacion compara
    # contra la regla que DEFECTOS.md declara.
    regla: str = ""


def clave(v: Any) -> str:
    """Minusculas, sin acentos y sin espacios de sobra: lo que hace norm_texto()."""
    d = unicodedata.normalize("NFD", str(v).lower())
    sin = "".join(c for c in d if unicodedata.category(c) != "Mn")
    return " ".join(sin.split())


def texto(v: Any) -> Valor:
    """Regla 3 y contrato §3: trim, colapso de dobles, y las formas de ausencia."""
    if v is None:
        return Valor(None)
    crudo = str(v)
    limpio = " ".join(crudo.split())
    if clave(limpio) in VACIOS:
        return Valor(None)
    if limpio != crudo:
        return Valor(limpio, f"«{crudo}» tenía espacios sobrantes", R_ESPACIOS)
    return Valor(limpio)


def numero(v: Any, regla: str = R_NUMERO) -> Valor:
    """Regla 6: celda vacia significa cero."""
    if isinstance(v, bool):
        raise Rechazo(regla, f"«{v}» no es un número")
    if isinstance(v, (int, float)):
        return Valor(v)
    if v is None or clave(v) in VACIOS:
        return Valor(0, "celda vacía se lee como cero", R_VACIO_CERO)
    raise Rechazo(regla, f"«{' '.join(str(v).split())}» no es un número")


def numero_opcional(v: Any, regla: str = R_NUMERO) -> Valor:
    """Como numero(), pero el vacio es NULL y no cero.

    Para los pesos: N4 los deja vacios porque captura la cantidad directa, y un
    cero ahi significaria que el frasco vacio pesa 0 g.
    """
    if isinstance(v, (int, float)) and not isinstance(v, bool):
        return Valor(v)
    if v is None or clave(v) in VACIOS:
        return Valor(None)
    raise Rechazo(regla, f"«{' '.join(str(v).split())}» no es un número")


def numero_inventario(v: Any) -> Valor:
    """Regla 10: 5311300206 y 05311300206 son el mismo numero.

    Normalizar ANTES de validar es lo que permite que la regla 10 vea el
    duplicado. Al reves, los dos entrarian y el unique no los atraparia.
    """
    t = texto(v)
    if t.dato is None or not t.dato.isdigit():
        return t
    sin_ceros = t.dato.lstrip("0") or "0"
    if sin_ceros != t.dato:
        return Valor(sin_ceros, f"«{t.dato}» llevaba ceros a la izquierda",
                     R_INVENTARIO)
    return t


def grado_nfpa(v: Any) -> Valor:
    if v is None:
        return Valor(None)
    c = clave(v).split(":")[0].strip()
    if c in GRADOS:
        return Valor(GRADOS[c])
    raise Rechazo("NFPA", f"«{v}» no es un grado NFPA del 0 al 4")


def color(v: Any) -> Valor:
    c = clave(v) if v is not None else ""
    if c in COLORES:
        return Valor(c)
    if c in SINONIMOS_COLOR:
        destino = SINONIMOS_COLOR[c]
        return Valor(destino, f"«{v}» no es un color; se lee como {destino}",
                     R_COLOR)
    if not c:
        raise Rechazo(R_COLOR, "sin color de almacenaje")
    raise Rechazo(R_COLOR, f"«{v}» no es uno de los seis colores")


def si_no(v: Any) -> Valor:
    if v is None:
        return Valor(None)
    c = clave(v)
    if c in ("si", "s", "true", "1"):
        return Valor(True)
    if c in ("no", "n", "false", "0"):
        return Valor(False)
    raise Rechazo("Sí/No", f"«{v}» no es Sí ni No")


def estado_fisico(solido: Any, liquido: Any, gas: Any) -> Valor:
    marcadas = [nombre for nombre, casilla in
                (("solido", solido), ("liquido", liquido), ("gas", gas))
                if casilla is not None and clave(casilla) == "x"]
    if len(marcadas) == 1:
        return Valor(marcadas[0])
    if not marcadas:
        raise Rechazo(R_ESTADO, "ninguna casilla de estado físico marcada")
    raise Rechazo(R_ESTADO,
                  f"{len(marcadas)} casillas marcadas: {', '.join(marcadas)}")


def funcionamiento(v: Any) -> Valor:
    c = clave(v) if v is not None else ""
    if c in FUNCIONAMIENTO:
        return Valor(FUNCIONAMIENTO[c])
    raise Rechazo(
        R_ENUM_FUNC,
        f"«{v}» no está en el enum funcionamiento_equipo: solo Correcto y "
        f"Presenta fallas")
```

`etl/rules/__init__.py`: vacio.

- [ ] **Step 4: Correr y ver que pasa**

Run: `python -m pytest etl/tests/test_normalizar.py -v`
Expected: PASS, 24 pruebas.

- [ ] **Step 5: Commit**

```bash
git add etl/rules/ etl/tests/test_normalizar.py
git commit -m "feat(etl): normalizaciones del formato unificado"
```

---

### Task 4: La validacion

**Files:**

- Create: `etl/rules/validar.py`
- Create: `etl/tests/test_validar.py`

**Interfaces:**

- Consumes: `etl.extract.formato.Hoja`, `etl.review.informe.Informe/Problema`,
  todo lo de `etl.rules.normalizar`.
- Produces: `Renglon` (frozen: `.fila: int`, `.campos: dict[str, Any]`),
  `Catalogos` (frozen: `.laboratorios: dict[str, set[str]]`), `Vistos`
  (mutable, vive toda la corrida: `.series`, `.inventarios`, `.unidades`),
  `validar(hoja, informe, catalogos, vistos) -> tuple[Renglon, ...] | None`
  (devuelve `None` si la hoja tuvo algun rechazo).

- [ ] **Step 1: Escribe la prueba que falla**

`etl/tests/test_validar.py`:

```python
from pathlib import Path

import pytest

from etl.extract.formato import leer
from etl.review.informe import Informe
from etl.rules.validar import Catalogos, Vistos, validar

LIMPIOS = Path("etl/ejemplos/datos-almacenes/limpios")
DEFECTOS = Path("etl/ejemplos/datos-almacenes/defectos")

# Espeja supabase/datos-iniciales.sql. Sin acentos, como esta en la base.
CATALOGOS = Catalogos(laboratorios={
    "N3": {"laboratorio de docencia n3 (por confirmar)"},
    "N4": {"analisis sensorial", "laboratorio de ensenianza 5"},
    "LUM": {"caracterizacion y procesamiento"},
    "LE": {"laboratorio de electronica"},
})


def _validar(ruta: Path, catalogos: Catalogos = CATALOGOS, vistos=None):
    inf = Informe()
    filas = validar(leer(ruta), inf, catalogos, vistos or Vistos())
    return filas, inf


def test_una_hoja_limpia_pasa_entera_y_sin_rechazos():
    filas, inf = _validar(LIMPIOS / "N3" / "N3-Reactivos.xlsx")
    assert filas is not None and len(filas) == 14
    assert inf.rechazos == []


def test_el_laboratorio_con_acentos_resuelve_contra_el_catalogo_sin_ellos():
    filas, inf = _validar(LIMPIOS / "LUM" / "LUM-Equipos.xlsx")
    assert filas is not None
    assert inf.rechazos == []


def test_un_laboratorio_de_otro_almacen_se_rechaza():
    # Defecto 2: N3 declara "Análisis Sensorial", que es de N4.
    filas, inf = _validar(DEFECTOS / "N3" / "N3-Equipos.xlsx")
    assert filas is None
    assert any("laboratorio" in p.regla.lower() or "laboratorio" in p.detalle.lower()
               for p in inf.rechazos)


def test_sin_catalogo_la_comprobacion_de_laboratorio_se_salta_y_lo_dice():
    filas, inf = _validar(DEFECTOS / "N3" / "N3-Equipos.xlsx",
                          catalogos=Catalogos(laboratorios={}))
    assert filas is not None                      # sin catalogo no hay como saberlo
    assert any(p.accion == "normalizado" and "catálogo" in p.detalle
               for p in inf.problemas)


def test_una_sub_ubicacion_que_no_es_del_almacen_se_rechaza():
    # Defecto 3: "N1-1" en un archivo de N3.
    filas, inf = _validar(DEFECTOS / "N3" / "N3-Reactivos.xlsx")
    assert filas is None
    assert any("N1-1" in str(p.valor) for p in inf.rechazos)


def test_los_ejemplos_sin_borrar_caen_por_su_sub_ubicacion():
    # Defecto 22: los tres ejemplos de N4/Material dicen N3 y LUM-2.
    filas, inf = _validar(DEFECTOS / "N4" / "N4-Material.xlsx")
    assert filas is None
    assert len([p for p in inf.rechazos if p.fila in (9, 10, 11)]) == 3


def test_una_serie_repetida_en_la_misma_hoja_se_rechaza():
    # Defecto 15: el texturometro toma la serie del colorimetro.
    filas, inf = _validar(DEFECTOS / "N4" / "N4-Equipos.xlsx")
    assert filas is None
    assert any("10238841" in str(p.valor) for p in inf.rechazos)


def test_el_mismo_articulo_en_dos_unidades_se_rechaza():
    # Defecto 20: hidroxido de sodio en g en N3/N4 y en kg en LUM.
    vistos = Vistos()
    _validar(LIMPIOS / "N3" / "N3-Reactivos.xlsx", vistos=vistos)
    filas, inf = _validar(DEFECTOS / "LUM" / "LUM-Reactivos.xlsx", vistos=vistos)
    assert filas is None
    assert any("kg" in str(p.valor) for p in inf.rechazos)


def test_los_pesos_invertidos_se_rechazan():
    # Defecto 19: vacio 640, lleno 92.
    filas, inf = _validar(DEFECTOS / "LUM" / "LUM-Reactivos.xlsx")
    assert filas is None
    assert any("peso" in p.regla.lower() or "peso" in p.detalle.lower()
               for p in inf.rechazos)


def test_un_renglon_con_varios_equipos_se_rechaza():
    # Defecto 14: "3 equipos iguales" en Observaciones.
    filas, inf = _validar(DEFECTOS / "LUM" / "LUM-Equipos.xlsx")
    assert filas is None
    assert any("equipo físico" in p.regla for p in inf.rechazos)


def test_los_espacios_sobrantes_se_corrigen_y_la_hoja_entra():
    # Defecto 8: normalizado, no rechazo.
    filas, inf = _validar(DEFECTOS / "N3" / "N3-Insumos.xlsx")
    assert filas is not None
    assert filas[0].campos["mueble"] == "Gabinete 309"
    assert len(inf.normalizados) >= 2
    assert inf.rechazos == []


def test_los_equipos_siempre_llevan_cantidad_uno():
    filas, _ = _validar(LIMPIOS / "N3" / "N3-Equipos.xlsx")
    assert all(r.campos["cantidad"] == 1 for r in filas)
```

- [ ] **Step 2: Correr y ver que falla**

Run: `python -m pytest etl/tests/test_validar.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'etl.rules.validar'`

- [ ] **Step 3: Implementar**

`etl/rules/validar.py`:

```python
"""Aplica las 13 reglas de captura y los choques con el esquema.

Devuelve los renglones normalizados, o None si la hoja tuvo algun rechazo: una
hoja entra entera o no entra. Todo lo que aqui se anota como `normalizado` ya
viene corregido en los renglones que se devuelven.

No conoce la base. Lo unico que no se puede comprobar sin ella es el
laboratorio, y por eso llega como argumento en `Catalogos`: sin conexion ese
diccionario va vacio, la comprobacion se salta y el informe lo dice.
"""

from __future__ import annotations

import re
from dataclasses import dataclass, field
from typing import Any

from etl.extract.formato import CAMPOS, Hoja
from etl.review.informe import Informe, Problema
from etl.rules import normalizar as n

R_SUB = "Ubicación · la sub-ubicación no es de este almacén"
R_LAB = "FK compuesta · (laboratorio_id, almacen_id)"
R_SERIE = "Regla 10 · la serie no se repite entre renglones"
R_INV = "Regla 10 · el número de inventario no se repite entre renglones"
R_PESO = "Regla 13 · el peso del frasco vacío va antes que el del lleno"
R_EQUIPO = "Regla 9 · un renglón por equipo físico"
R_UNIDAD = "Contrato §6 · la unidad se valida contra articulo.unidad_base"

# Regla 9: la cuenta de equipos metida en Observaciones es la senial de que un
# renglon representa varios. La regla 1 ya manda los numeros fuera de las
# columnas de texto, asi que aqui no puede haber una cantidad legitima.
VARIOS_EQUIPOS = re.compile(r"\b(\d+)\s+equipos?\b", re.IGNORECASE)


@dataclass(frozen=True)
class Catalogos:
    """Lo que solo existe en la base. Vacio = no hay conexion, se salta."""
    laboratorios: dict[str, set[str]] = field(default_factory=dict)


@dataclass
class Vistos:
    """Estado que cruza archivos dentro de una corrida."""
    series: dict[str, str] = field(default_factory=dict)
    inventarios: dict[str, str] = field(default_factory=dict)
    unidades: dict[str, tuple[str, str]] = field(default_factory=dict)


@dataclass(frozen=True)
class Renglon:
    fila: int
    campos: dict[str, Any]


def _sub_es_del_almacen(sub: str | None, almacen: str) -> bool:
    return sub is None or sub == almacen or sub.startswith(f"{almacen}-")


def validar(hoja: Hoja, informe: Informe, catalogos: Catalogos,
            vistos: Vistos) -> tuple[Renglon, ...] | None:
    letras = CAMPOS[hoja.nombre]
    salida: list[Renglon] = []
    hubo_rechazo = False

    def anota(fila, campo, regla, valor, accion, detalle):
        informe.anotar(Problema(
            archivo=hoja.archivo, hoja=hoja.nombre, fila=fila,
            columna=letras.get(campo, ""), regla=regla, valor=valor,
            accion=accion, detalle=detalle))

    if hoja.nombre == "Equipos" and not catalogos.laboratorios:
        anota(None, "laboratorio", R_LAB, "", "normalizado",
              "sin catálogo de laboratorios: la comprobación se saltó")

    for fila, crudo in zip(hoja.filas, hoja.renglones):
        campos: dict[str, Any] = {}
        rechazado = False

        def aplica(campo, funcion):
            """Corre un normalizador sobre el campo y anota lo que salga."""
            nonlocal rechazado, hubo_rechazo
            try:
                v = funcion(crudo.get(campo))
            except n.Rechazo as r:
                anota(fila, campo, r.regla, crudo.get(campo), "rechazo", r.detalle)
                rechazado = hubo_rechazo = True
                return None
            if v.aviso:
                anota(fila, campo, v.regla or "Normalización", crudo.get(campo),
                      "normalizado", v.aviso)
            campos[campo] = v.dato
            return v.dato

        # --- comunes a todas las hojas -------------------------------------
        for campo in ("marca", "modelo", "presentacion", "especificacion",
                      "familia", "mueble", "repisa", "fila_cajon", "coord_h",
                      "coord_v", "coord_i", "observaciones", "sub_ubicacion",
                      "clasificacion", "unidad", "metodo_conservacion",
                      "temperatura", "fecha_recoleccion", "fecha_preparacion",
                      "responsable_muestra", "origen_especie", "laboratorio",
                      "mantenimiento", "fecha_chequeo", "peligro_especial",
                      "caracteristica_quimica", "caracteristica_toxica"):
            if campo in letras:
                aplica(campo, n.texto)

        sub = campos.get("sub_ubicacion")
        if not _sub_es_del_almacen(sub, hoja.almacen):
            anota(fila, "sub_ubicacion", R_SUB, sub, "rechazo",
                  f"«{sub}» no es una sub-ubicación de {hoja.almacen}")
            rechazado = hubo_rechazo = True

        # --- el nombre del articulo ----------------------------------------
        campo_nombre = "sustancia" if hoja.nombre == "Reactivos" else "articulo"
        nombre = aplica(campo_nombre, n.texto)

        # --- cantidad y unidad ---------------------------------------------
        if hoja.nombre == "Equipos":
            # Regla 9: un renglon por equipo fisico, asi que siempre es 1.
            campos["cantidad"] = 1
            campos["unidad"] = "pieza"
        else:
            aplica("cantidad", n.numero)
            unidad = campos.get("unidad")
            if nombre and unidad:
                antes = vistos.unidades.get(nombre)
                if antes and antes[0] != unidad:
                    anota(fila, "unidad", R_UNIDAD, unidad, "rechazo",
                          f"el mismo artículo está en «{antes[0]}» en {antes[1]}")
                    rechazado = hubo_rechazo = True
                else:
                    vistos.unidades[nombre] = (unidad, hoja.archivo)

        # --- por hoja -------------------------------------------------------
        if hoja.nombre == "Reactivos":
            aplica("color", n.color)
            aplica("hoja_seguridad", n.si_no)
            aplica("implica_peligro", n.si_no)
            for g in ("riesgo_salud", "riesgo_reactividad",
                      "riesgo_inflamabilidad"):
                aplica(g, n.grado_nfpa)
            aplica("peso_vacio", n.numero_opcional)
            aplica("peso_total", n.numero_opcional)
            try:
                campos["estado_fisico"] = n.estado_fisico(
                    crudo.get("solido"), crudo.get("liquido"),
                    crudo.get("gas")).dato
            except n.Rechazo as r:
                anota(fila, "solido", r.regla, "", "rechazo", r.detalle)
                rechazado = hubo_rechazo = True

            vacio, lleno = campos.get("peso_vacio"), campos.get("peso_total")
            if vacio is not None and lleno is not None and lleno <= vacio:
                anota(fila, "peso_vacio", R_PESO, f"{vacio} / {lleno}", "rechazo",
                      f"el peso lleno ({lleno}) no supera al vacío ({vacio})")
                rechazado = hubo_rechazo = True

        if hoja.nombre == "Equipos":
            aplica("funcionamiento", n.funcionamiento)
            aplica("numero_serie", n.texto)
            aplica("numero_inventario", n.numero_inventario)

            lab = campos.get("laboratorio")
            if lab and catalogos.laboratorios:
                validos = catalogos.laboratorios.get(hoja.almacen, set())
                if n.clave(lab) not in validos:
                    anota(fila, "laboratorio", R_LAB, lab, "rechazo",
                          f"«{lab}» no es un laboratorio de {hoja.almacen}")
                    rechazado = hubo_rechazo = True

            obs = campos.get("observaciones") or ""
            encontrado = VARIOS_EQUIPOS.search(obs)
            if encontrado and int(encontrado.group(1)) > 1:
                anota(fila, "observaciones", R_EQUIPO, obs, "rechazo",
                      "un renglón por equipo físico, con su serie y su inventario")
                rechazado = hubo_rechazo = True

            for campo, visto, regla in (("numero_serie", vistos.series, R_SERIE),
                                        ("numero_inventario", vistos.inventarios,
                                         R_INV)):
                valor = campos.get(campo)
                if valor is None:
                    continue
                if valor in visto:
                    anota(fila, campo, regla, valor, "rechazo",
                          f"ya estaba en {visto[valor]}")
                    rechazado = hubo_rechazo = True
                else:
                    visto[valor] = f"{hoja.archivo} fila {fila}"

        if not rechazado:
            salida.append(Renglon(fila=fila, campos=campos))

    return None if hubo_rechazo else tuple(salida)
```

- [ ] **Step 4: Correr y ver que pasa**

Run: `python -m pytest etl/tests/test_validar.py -v`
Expected: PASS, 12 pruebas.

- [ ] **Step 5: Commit**

```bash
git add etl/rules/validar.py etl/tests/test_validar.py
git commit -m "feat(etl): validacion de las 13 reglas"
```

---

### Task 5: El simulacro — CLI en seco y prueba de aceptacion

**Files:**

- Create: `etl/cargar.py`
- Create: `etl/tests/test_aceptacion.py`

**Interfaces:**

- Consumes: `leer`, `Informe`, `validar`, `Catalogos`, `Vistos`.
- Produces: `ORDEN: tuple[str, ...]` (`("N3","N4","LUM","LE")`),
  `archivos(origen: Path) -> list[Path]` en orden fijo,
  `simular(origen: Path, catalogos: Catalogos) -> tuple[Informe, dict[str, tuple[Renglon, ...]]]`,
  `main(argv=None) -> int`.

- [ ] **Step 1: Escribe la prueba que falla**

`etl/tests/test_aceptacion.py`:

```python
"""La prueba que importa: el informe contra DEFECTOS.md.

DEFECTOS.md se genera de la misma lista que produce los archivos de defectos,
asi que es un oraculo que no puede desfasarse de ellos.
"""

import re
import sys
from pathlib import Path

from etl.cargar import archivos, simular
from etl.rules.validar import Catalogos

sys.path.insert(0, "etl/ejemplos")
import generar  # noqa: E402

LIMPIOS = Path("etl/ejemplos/datos-almacenes/limpios")
DEFECTOS = Path("etl/ejemplos/datos-almacenes/defectos")

CATALOGOS = Catalogos(laboratorios={
    "N3": {"laboratorio de docencia n3 (por confirmar)"},
    "N4": {"analisis sensorial", "laboratorio de ensenianza 5"},
    "LUM": {"caracterizacion y procesamiento"},
    "LE": {"laboratorio de electronica"},
})


def test_encuentra_los_15_archivos_en_orden_fijo():
    rutas = archivos(LIMPIOS)
    assert len(rutas) == 15
    assert [r.parent.name for r in rutas][:5] == ["N3"] * 5
    assert rutas[-1].parent.name == "LE"


def test_el_juego_limpio_no_produce_un_solo_rechazo():
    informe, renglones = simular(LIMPIOS, CATALOGOS)
    assert informe.rechazos == [], [p.detalle for p in informe.rechazos]
    assert sum(len(r) for r in renglones.values()) == 164


def test_cada_defecto_documentado_produce_un_rechazo_o_un_aviso():
    informe, _ = simular(DEFECTOS, CATALOGOS)

    # (archivo, hoja, fila de Excel) de cada problema detectado
    detectados = {(p.archivo, p.hoja, p.fila) for p in informe.problemas}

    faltantes = []
    for d in generar.DEFECTOS:
        archivo = generar.archivo(d.almacen, d.hoja)
        salto = generar.desplazamiento(d.almacen, d.hoja)
        filas = {d.fila_excel(i + salto) for i, _, _ in d.celdas}
        if d.columna_extra:
            continue          # una columna de mas no rompe ningun renglon
        if d.conservar_ejemplos:
            filas = set(range(generar.HOJAS[d.hoja].primera_fila,
                              generar.HOJAS[d.hoja].primera_fila
                              + generar.HOJAS[d.hoja].ejemplos))
        if not any((archivo, d.hoja, f) in detectados for f in filas):
            faltantes.append(f"{d.num}. {d.regla} — {archivo} {sorted(filas)}")

    assert not faltantes, "defectos que el ETL no vio:\n" + "\n".join(faltantes)


def test_no_inventa_problemas_en_renglones_sanos():
    informe, _ = simular(DEFECTOS, CATALOGOS)
    esperadas = set()
    for d in generar.DEFECTOS:
        archivo = generar.archivo(d.almacen, d.hoja)
        salto = generar.desplazamiento(d.almacen, d.hoja)
        for i, _, _ in d.celdas:
            esperadas.add((archivo, d.fila_excel(i + salto)))
        if d.conservar_ejemplos:
            for f in range(generar.HOJAS[d.hoja].primera_fila,
                           generar.HOJAS[d.hoja].primera_fila
                           + generar.HOJAS[d.hoja].ejemplos):
                esperadas.add((archivo, f))

    sobrantes = [f"{p.archivo} fila {p.fila}: {p.regla} — {p.detalle}"
                 for p in informe.problemas
                 if p.fila is not None and (p.archivo, p.fila) not in esperadas]
    assert not sobrantes, "problemas en renglones que deberían estar sanos:\n" \
                          + "\n".join(sobrantes)
```

- [ ] **Step 2: Correr y ver que falla**

Run: `python -m pytest etl/tests/test_aceptacion.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'etl.cargar'`

- [ ] **Step 3: Implementar el CLI**

`etl/cargar.py`:

```python
"""El comando. Sin --cargar no escribe nada.

    python -m etl.cargar --juego limpios              simulacro
    python -m etl.cargar --juego defectos             el informe con los 22
    python -m etl.cargar --juego limpios --cargar     escribe de verdad
"""

from __future__ import annotations

import argparse
import datetime as dt
import sys
from pathlib import Path

from etl.extract.formato import leer, slug
from etl.review.informe import Informe
from etl.rules.validar import Catalogos, Renglon, Vistos, validar

RAIZ = Path(__file__).resolve().parents[1]
EJEMPLOS = RAIZ / "etl" / "ejemplos" / "datos-almacenes"
REVIEW = RAIZ / "etl" / "review"

# Fijo, para que la corrida sea reproducible. El orden importa: `articulo` es
# global, asi que la deduplicacion solo se ejercita con varios almacenes juntos.
ORDEN = ("N3", "N4", "LUM", "LE")
ORDEN_HOJAS = ("Reactivos", "Insumos", "Material", "Equipos",
               "Materia biológica", "Electrónica")


def archivos(origen: Path) -> list[Path]:
    """Los archivos que hay, en orden fijo.

    El emparejamiento es por slug COMPLETO, no por prefijo: «Material» y
    «Materia biológica» comparten las seis primeras letras y un startswith los
    cruzaria.
    """
    rutas: list[Path] = []
    for clave in ORDEN:
        carpeta = origen / clave
        if not carpeta.is_dir():
            continue
        por_hoja = {r.stem.partition("-")[2].lower(): r
                    for r in carpeta.glob("*.xlsx")
                    if not r.name.startswith("~$")}
        for hoja in ORDEN_HOJAS:
            ruta = por_hoja.get(slug(hoja))
            if ruta is not None:
                rutas.append(ruta)
    return rutas


def simular(origen: Path,
            catalogos: Catalogos) -> tuple[Informe, dict[str, tuple[Renglon, ...]]]:
    informe, vistos = Informe(), Vistos()
    renglones: dict[str, tuple[Renglon, ...]] = {}
    for ruta in archivos(origen):
        hoja = leer(ruta)
        validos = validar(hoja, informe, catalogos, vistos)
        if validos is not None:
            renglones[ruta.name] = validos
    return informe, renglones


def main(argv: list[str] | None = None) -> int:
    p = argparse.ArgumentParser(description="Carga el formato unificado.")
    p.add_argument("--juego", choices=("limpios", "defectos"))
    p.add_argument("--origen", type=Path)
    p.add_argument("--almacen", choices=ORDEN)
    p.add_argument("--cargar", action="store_true",
                   help="escribe en la base; sin esto es simulacro")
    p.add_argument("--dsn", help="por omisión, DATABASE_URL")
    args = p.parse_args(argv)

    origen = args.origen or (EJEMPLOS / (args.juego or "limpios"))
    if args.almacen:
        origen_almacen = origen / args.almacen
        if not origen_almacen.is_dir():
            print(f"No hay carpeta {origen_almacen}", file=sys.stderr)
            return 2

    catalogos = Catalogos()
    informe, renglones = simular(origen, catalogos)

    ruta = REVIEW / f"{dt.date.today():%Y-%m-%d}-{origen.name}.csv"
    informe.a_csv(ruta)

    total = sum(len(r) for r in renglones.values())
    print(f"  {len(renglones)} hojas listas, {total} renglones")
    print(f"  {len(informe.rechazos)} rechazos, "
          f"{len(informe.normalizados)} normalizados → {ruta}")

    if informe.rechazos:
        return 1
    if not args.cargar:
        print("  simulacro: no se escribió nada. Agrega --cargar.")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 4: Correr las pruebas**

Run: `python -m pytest etl/tests -v`
Expected: PASS, todas. Si `test_cada_defecto_documentado_produce_un_rechazo_o_un_aviso`
falla, la lista de faltantes dice exactamente que defecto no se ve y en que
archivo y fila; corregir `validar.py`, **nunca los fixtures**.

- [ ] **Step 5: Correr el comando a mano**

```bash
python -m etl.cargar --juego limpios
python -m etl.cargar --juego defectos
```

Expected: el primero, 15 hojas y 164 renglones, 0 rechazos, codigo 0.
El segundo, codigo 1 y un CSV en `etl/review/` con los problemas.

- [ ] **Step 6: Commit**

```bash
git add etl/cargar.py etl/tests/test_aceptacion.py
git commit -m "feat(etl): simulacro sin base y prueba contra DEFECTOS.md"
```

---

### Task 6: Conexion y el perfil que firma

**Files:**

- Create: `etl/db.py`
- Create: `etl/tests/test_db.py`
- Modify: `supabase/seed.sql`

**Interfaces:**

- Consumes: nada del ETL.
- Produces: `dsn(explicito: str | None) -> str`, `conectar(dsn) -> Connection`,
  `perfil_de_carga(cur) -> uuid.UUID`, `catalogos(cur) -> Catalogos`,
  `almacenes(cur) -> dict[str, int]`, `laboratorios(cur) -> dict[tuple[str,str], int]`,
  `FaltaPerfilDeCarga(Exception)`.

- [ ] **Step 1: Agregar el perfil al seed local**

En `supabase/seed.sql`, en la lista de usuarios de prueba (junto a
`admin@uaeh.local`), agregar una fila mas:

```sql
      ('carga@uaeh.local',   'Cargador de inventarios', 'admin'::public.rol_usuario, null::text),
```

Comentario encima de la lista:

```sql
    -- carga@uaeh.local es la identidad del ETL. `movimiento.usuario_id` es
    -- not null y el trigger cae a auth.uid(), que por psycopg es NULL: sin un
    -- perfil propio el cargador no puede insertar un solo movimiento. Va
    -- aparte de admin@ para que los movimientos de carga_inicial se
    -- distingan de lo que capturo una persona.
```

Correr: `supabase db reset`
Verificar: `psql "$DATABASE_URL" -c "select nombre, rol from public.perfil where id = (select id from auth.users where email = 'carga@uaeh.local')"`
Expected: una fila, rol `admin`.

- [ ] **Step 2: Escribe la prueba que falla**

`etl/tests/test_db.py`:

```python
import os

import pytest

from etl import db

pytestmark = pytest.mark.skipif(
    not os.environ.get("DATABASE_URL"),
    reason="necesita Supabase local: supabase start && supabase db reset")


@pytest.fixture
def cur():
    with db.conectar(db.dsn(None)) as con, con.cursor() as c:
        yield c
        con.rollback()


def test_encuentra_el_perfil_de_carga(cur):
    assert db.perfil_de_carga(cur) is not None


def test_los_cuatro_almacenes_estan(cur):
    assert set(db.almacenes(cur)) == {"N3", "N4", "LUM", "LE"}


def test_los_laboratorios_llegan_normalizados_sin_acentos(cur):
    cat = db.catalogos(cur)
    assert "caracterizacion y procesamiento" in cat.laboratorios["LUM"]
    assert "analisis sensorial" in cat.laboratorios["N4"]
```

- [ ] **Step 3: Correr y ver que falla**

Run: `DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres python -m pytest etl/tests/test_db.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'etl.db'`

- [ ] **Step 4: Implementar**

`etl/db.py`:

```python
"""Conexion, la identidad que firma la carga, y los catalogos de la base.

Se conecta como `postgres`, no con la anon key: una carga administrativa se
salta la RLS por construccion, y hace falta transaccion multi-sentencia, que
PostgREST no da.
"""

from __future__ import annotations

import os
import uuid

import psycopg

from etl.rules.normalizar import clave
from etl.rules.validar import Catalogos

CORREO_DE_CARGA = "carga@uaeh.local"

# Lo que imprime `supabase start`.
DSN_LOCAL = "postgresql://postgres:postgres@127.0.0.1:54322/postgres"


class FaltaPerfilDeCarga(Exception):
    pass


def dsn(explicito: str | None = None) -> str:
    return explicito or os.environ.get("DATABASE_URL") or DSN_LOCAL


def conectar(cadena: str) -> psycopg.Connection:
    return psycopg.connect(cadena)


def perfil_de_carga(cur) -> uuid.UUID:
    cur.execute(
        """
        select p.id from public.perfil p
          join auth.users u on u.id = p.id
         where u.email = %s
        """,
        (CORREO_DE_CARGA,))
    fila = cur.fetchone()
    if fila is None:
        raise FaltaPerfilDeCarga(
            f"No existe el perfil {CORREO_DE_CARGA}. En local sale de "
            f"supabase/seed.sql (supabase db reset); en remoto se da de alta "
            f"desde el dashboard de Auth. Sin el, movimiento.usuario_id es "
            f"not null y ningun movimiento se puede insertar.")
    return fila[0]


def almacenes(cur) -> dict[str, int]:
    cur.execute("select clave, id from public.almacen")
    return dict(cur.fetchall())


def laboratorios(cur) -> dict[tuple[str, str], int]:
    """(clave de almacen, nombre normalizado) -> id."""
    cur.execute(
        """
        select a.clave, l.nombre, l.id
          from public.laboratorio l
          join public.almacen a on a.id = l.almacen_id
        """)
    return {(c, clave(nombre)): ident for c, nombre, ident in cur.fetchall()}


def catalogos(cur) -> Catalogos:
    labs: dict[str, set[str]] = {}
    for (almacen, nombre) in laboratorios(cur):
        labs.setdefault(almacen, set()).add(nombre)
    return Catalogos(laboratorios=labs)
```

- [ ] **Step 5: Correr y ver que pasa**

Run: `DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres python -m pytest etl/tests/test_db.py -v`
Expected: PASS, 3 pruebas.

- [ ] **Step 6: Commit**

```bash
git add etl/db.py etl/tests/test_db.py supabase/seed.sql
git commit -m "feat(etl): conexion y el perfil carga@uaeh.local"
```

---

### Task 7: Resolver o crear el articulo

**Files:**

- Create: `etl/catalogo.py`
- Create: `etl/tests/test_catalogo.py`

**Interfaces:**

- Consumes: `etl.db`.
- Produces: `resolver(cur, *, nombre, descripcion, unidad, clasificacion,
  familia=None, perfil_id) -> Resolucion`; `Resolucion` (frozen:
  `.articulo_id: int`, `.creado: bool`, `.parecidos: list[tuple[int, str, float]]`).

- [ ] **Step 1: Escribe la prueba que falla**

`etl/tests/test_catalogo.py`:

```python
import os

import pytest

from etl import catalogo, db

pytestmark = pytest.mark.skipif(
    not os.environ.get("DATABASE_URL"),
    reason="necesita Supabase local: supabase start && supabase db reset")

ZINC_95 = "Zinc en polvo, sólido, pureza 95%, presentación 500 g, CAS: 7440-66-6"
ZINC_93 = "Zinc en polvo, sólido, pureza 93%, presentación 500 g, CAS: 7440-66-6"


@pytest.fixture
def cur():
    with db.conectar(db.dsn(None)) as con, con.cursor() as c:
        yield c
        con.rollback()          # cada prueba deja la base como estaba


def _resolver(cur, nombre, unidad="g", descripcion=None):
    return catalogo.resolver(
        cur, nombre=nombre, descripcion=descripcion, unidad=unidad,
        clasificacion="reactivo", perfil_id=db.perfil_de_carga(cur))


def test_la_primera_vez_lo_crea_sin_verificar(cur):
    r = _resolver(cur, ZINC_95)
    assert r.creado is True
    cur.execute("select verificado from public.articulo where id = %s",
                (r.articulo_id,))
    assert cur.fetchone()[0] is False


def test_la_segunda_vez_devuelve_el_mismo(cur):
    primero = _resolver(cur, ZINC_95)
    segundo = _resolver(cur, ZINC_95)
    assert segundo.articulo_id == primero.articulo_id
    assert segundo.creado is False


def test_el_zinc_al_95_y_al_93_son_dos_articulos(cur):
    # El caso del comentario de catalogo.sql: mismo CAS, la pureza cambia la
    # sustancia. Su similitud por trigramas es altisima; si el cargador
    # emparejara por parecido, los fusionaria en silencio.
    a = _resolver(cur, ZINC_95)
    b = _resolver(cur, ZINC_93)
    assert a.articulo_id != b.articulo_id
    assert b.creado is True
    assert b.parecidos, "buscar_articulo() debería avisar del parecido"


def test_el_mismo_nombre_en_otra_unidad_es_otro_articulo(cur):
    a = _resolver(cur, ZINC_95, unidad="g")
    b = _resolver(cur, ZINC_95, unidad="kg")
    assert a.articulo_id != b.articulo_id


def test_guarda_el_texto_crudo_como_alias_de_migracion(cur):
    r = _resolver(cur, ZINC_95)
    cur.execute(
        "select origen from public.articulo_alias "
        " where articulo_id = %s and texto = %s", (r.articulo_id, ZINC_95))
    assert cur.fetchone()[0] == "migracion"


def test_un_alias_existente_resuelve_al_articulo(cur):
    r = _resolver(cur, ZINC_95)
    cur.execute(
        "insert into public.articulo_alias (articulo_id, texto, origen) "
        "values (%s, %s, 'busqueda')", (r.articulo_id, "Zinc polvo 95"))
    otro = _resolver(cur, "Zinc polvo 95")
    assert otro.articulo_id == r.articulo_id
    assert otro.creado is False
```

- [ ] **Step 2: Correr y ver que falla**

Run: `DATABASE_URL=... python -m pytest etl/tests/test_catalogo.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'etl.catalogo'`

- [ ] **Step 3: Implementar**

`etl/catalogo.py`:

```python
"""Resolver un articulo, o crearlo.

SOLO por coincidencia exacta: primero la identidad
(nombre_canonico, descripcion, unidad_base), que es la llave unica de la tabla;
despues articulo_alias.texto exacto.

buscar_articulo() se llama, pero para AVISAR, no para decidir. Compara por
trigramas, y estas dos cadenas tienen similitud altisima:

    Zinc en polvo, solido, pureza 95%, presentacion 500 g, CAS: 7440-66-6
    Zinc en polvo, solido, pureza 93%, presentacion 500 g, CAS: 7440-66-6

y son DOS articulos: la pureza cambia la sustancia. Auto-aceptar la mejor
coincidencia los fusionaria en silencio, con las cantidades sumadas, y eso no
lanza ningun error ni se ve en ninguna pantalla. Fusionar es una decision
humana, y el esquema le dio funcion propia: public.fusionar_articulo().
"""

from __future__ import annotations

import uuid
from dataclasses import dataclass, field


@dataclass(frozen=True)
class Resolucion:
    articulo_id: int
    creado: bool
    parecidos: list[tuple[int, str, float]] = field(default_factory=list)


def _por_identidad(cur, nombre: str, descripcion: str | None,
                   unidad: str) -> int | None:
    cur.execute(
        """
        select id from public.articulo
         where nombre_canonico is not distinct from %s
           and descripcion     is not distinct from %s
           and unidad_base     is not distinct from %s
        """,
        (nombre, descripcion, unidad))
    fila = cur.fetchone()
    return fila[0] if fila else None


def _por_alias(cur, texto: str) -> int | None:
    cur.execute("select articulo_id from public.articulo_alias where texto = %s",
                (texto,))
    fila = cur.fetchone()
    return fila[0] if fila else None


def _parecidos(cur, nombre: str) -> list[tuple[int, str, float]]:
    cur.execute(
        "select articulo_id, nombre_canonico, similitud "
        "  from public.buscar_articulo(%s, 0.4, 5)", (nombre,))
    return list(cur.fetchall())


def _guardar_alias(cur, articulo_id: int, texto: str) -> None:
    cur.execute(
        "insert into public.articulo_alias (articulo_id, texto, origen) "
        "values (%s, %s, 'migracion') on conflict (articulo_id, texto) do nothing",
        (articulo_id, texto))


def resolver(cur, *, nombre: str, descripcion: str | None, unidad: str,
             clasificacion: str, familia: str | None = None,
             perfil_id: uuid.UUID) -> Resolucion:
    existente = _por_identidad(cur, nombre, descripcion, unidad)
    if existente is None:
        existente = _por_alias(cur, nombre)

    if existente is not None:
        _guardar_alias(cur, existente, nombre)
        return Resolucion(articulo_id=existente, creado=False)

    parecidos = _parecidos(cur, nombre)

    cur.execute(
        """
        insert into public.articulo
          (nombre_canonico, descripcion, clasificacion, unidad_base, familia,
           verificado, creado_por)
        values (%s, %s, %s, %s, %s, false, %s)
        returning id
        """,
        (nombre, descripcion, clasificacion, unidad, familia, perfil_id))
    nuevo = cur.fetchone()[0]
    _guardar_alias(cur, nuevo, nombre)
    return Resolucion(articulo_id=nuevo, creado=True, parecidos=parecidos)
```

- [ ] **Step 4: Correr y ver que pasa**

Run: `DATABASE_URL=... python -m pytest etl/tests/test_catalogo.py -v`
Expected: PASS, 6 pruebas.

- [ ] **Step 5: Commit**

```bash
git add etl/catalogo.py etl/tests/test_catalogo.py
git commit -m "feat(etl): resolver articulos por identidad exacta, nunca por parecido"
```

---

### Task 8: Escribir en la base

**Files:**

- Create: `etl/destino.py`
- Create: `etl/tests/test_destino.py`

**Interfaces:**

- Consumes: `etl.db`, `etl.catalogo`, `etl.rules.validar.Renglon`,
  `etl.extract.formato.Hoja`.
- Produces: `etiqueta_de(componentes: dict) -> str`,
  `upsert_ubicacion(cur, almacen_id, componentes: dict) -> int | None`,
  `crear_carga(cur, hoja, almacen_id, perfil_id, filas) -> int`,
  `escribir_hoja(cur, hoja, renglones, perfil_id) -> int` (devuelve cuantas
  existencias NUEVAS se crearon; 0 si todas ya estaban).

- [ ] **Step 1: Escribe la prueba que falla**

`etl/tests/test_destino.py`:

```python
import os
from pathlib import Path

import pytest

from etl import db, destino
from etl.extract.formato import leer
from etl.review.informe import Informe
from etl.rules.validar import Catalogos, Vistos, validar

pytestmark = pytest.mark.skipif(
    not os.environ.get("DATABASE_URL"),
    reason="necesita Supabase local: supabase start && supabase db reset")

LIMPIOS = Path("etl/ejemplos/datos-almacenes/limpios")


@pytest.fixture
def cur():
    with db.conectar(db.dsn(None)) as con, con.cursor() as c:
        yield c
        con.rollback()


def test_la_etiqueta_de_ubicacion_es_estable():
    a = destino.etiqueta_de({"sub_ubicacion": "N3", "mueble": "Anaquel 2",
                             "repisa": "3", "fila_cajon": "4"})
    b = destino.etiqueta_de({"fila_cajon": "4", "repisa": "3",
                             "mueble": "Anaquel 2", "sub_ubicacion": "N3"})
    assert a == b == "N3 · Anaquel 2 · Repisa 3 · Fila 4"


def test_la_misma_ubicacion_dos_veces_es_una_sola_fila(cur):
    alm = db.almacenes(cur)["N3"]
    comp = {"sub_ubicacion": "N3", "mueble": "Anaquel 2", "repisa": "3"}
    assert destino.upsert_ubicacion(cur, alm, comp) == \
           destino.upsert_ubicacion(cur, alm, dict(comp))


def test_carga_una_hoja_entera_con_su_movimiento(cur):
    hoja = leer(LIMPIOS / "N3" / "N3-Insumos.xlsx")
    filas = validar(hoja, Informe(), db.catalogos(cur), Vistos())
    escritas = destino.escribir_hoja(cur, hoja, filas, db.perfil_de_carga(cur))

    assert escritas == 12
    cur.execute("select count(*) from public.existencia")
    assert cur.fetchone()[0] == 12
    # Los guantes de latex estan en 0: sin movimiento, y agotados.
    cur.execute("select count(*) from public.movimiento where tipo='carga_inicial'")
    assert cur.fetchone()[0] == 11
    cur.execute("select count(*) from public.existencia where estado = 'agotado'")
    assert cur.fetchone()[0] == 1


def test_cargar_dos_veces_no_duplica(cur):
    hoja = leer(LIMPIOS / "N3" / "N3-Insumos.xlsx")
    filas = validar(hoja, Informe(), db.catalogos(cur), Vistos())
    perfil = db.perfil_de_carga(cur)
    destino.escribir_hoja(cur, hoja, filas, perfil)
    segunda = destino.escribir_hoja(cur, hoja, filas, perfil)

    assert segunda == 0
    cur.execute("select count(*) from public.existencia")
    assert cur.fetchone()[0] == 12


def test_el_movimiento_lleva_el_perfil_del_cargador(cur):
    hoja = leer(LIMPIOS / "N3" / "N3-Insumos.xlsx")
    filas = validar(hoja, Informe(), db.catalogos(cur), Vistos())
    perfil = db.perfil_de_carga(cur)
    destino.escribir_hoja(cur, hoja, filas, perfil)

    cur.execute("select distinct usuario_id from public.movimiento")
    assert cur.fetchall() == [(perfil,)]
```

- [ ] **Step 2: Correr y ver que falla**

Run: `DATABASE_URL=... python -m pytest etl/tests/test_destino.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'etl.destino'`

- [ ] **Step 3: Implementar**

`etl/destino.py`:

```python
"""Escribe carga, ubicacion, existencia y movimiento.

Una transaccion por archivo la abre quien llama: aqui todo corre sobre el mismo
cursor y no se hace commit.
"""

from __future__ import annotations

import json
import uuid
from typing import Any

from etl import catalogo, db
from etl.extract.formato import Hoja
from etl.rules.normalizar import clave
from etl.rules.validar import Renglon

# La clasificacion sale de la hoja, salvo en las dos que traen columna propia.
CLASIFICACION_POR_HOJA = {
    "Reactivos": "reactivo",
    "Equipos": "equipo",
    "Materia biológica": "materia_biologica",
}
CLASIFICACION_POR_TEXTO = {
    "material": "material", "insumo": "insumo", "equipo": "equipo",
    "reactivo": "reactivo", "materia biologica": "materia_biologica",
    "componente": "componente",
}

PARTES = (("sub_ubicacion", ""), ("mueble", ""), ("repisa", "Repisa "),
          ("fila_cajon", "Fila "), ("coord_h", "H"), ("coord_v", "V"),
          ("coord_i", "I"))


def etiqueta_de(componentes: dict[str, Any]) -> str:
    """Legible y estable: el orden sale de PARTES, no del dict."""
    trozos = [f"{prefijo}{componentes[campo]}"
              for campo, prefijo in PARTES
              if componentes.get(campo) not in (None, "")]
    return " · ".join(trozos)


def upsert_ubicacion(cur, almacen_id: int,
                     componentes: dict[str, Any]) -> int | None:
    limpios = {k: str(v) for k, v in componentes.items()
               if k in dict(PARTES) and v not in (None, "")}
    if not limpios:
        return None
    cur.execute(
        """
        insert into public.ubicacion (almacen_id, etiqueta, componentes)
        values (%s, %s, %s)
        on conflict (almacen_id, etiqueta) do update set etiqueta = excluded.etiqueta
        returning id
        """,
        (almacen_id, etiqueta_de(componentes), json.dumps(limpios)))
    return cur.fetchone()[0]


def crear_carga(cur, hoja: Hoja, almacen_id: int, perfil_id: uuid.UUID,
                filas: int) -> int:
    cur.execute(
        """
        insert into public.carga
          (almacen_id, archivo, hoja, periodo, actualizado_el, responsable,
           filas, cargado_por)
        values (%s, %s, %s, %s, %s, %s, %s, %s) returning id
        """,
        (almacen_id, hoja.archivo, hoja.nombre, hoja.periodo,
         _fecha(hoja.actualizado), hoja.responsable, filas, perfil_id))
    return cur.fetchone()[0]


def _fecha(texto: str | None):
    """El formato la trae como dd/mm/aaaa; Postgres la quiere ISO."""
    if not texto or "/" not in texto:
        return None
    d, m, a = texto.split("/")
    return f"{a}-{m}-{d}"


def _clasificacion(hoja: Hoja, campos: dict[str, Any]) -> str:
    if hoja.nombre in CLASIFICACION_POR_HOJA:
        return CLASIFICACION_POR_HOJA[hoja.nombre]
    return CLASIFICACION_POR_TEXTO[clave(campos.get("clasificacion") or "insumo")]


def _existencia_previa(cur, hoja: Hoja, almacen_id: int, articulo_id: int,
                       ubicacion_id: int | None, campos: dict[str, Any]) -> int | None:
    """Punto 9 del contrato: la llave natural de una existencia."""
    if hoja.nombre == "Equipos":
        serie, inv = campos.get("numero_serie"), campos.get("numero_inventario")
        if serie is None and inv is None:
            return None
        cur.execute(
            "select id from public.existencia "
            " where (numero_serie is not null and numero_serie = %s) "
            "    or (numero_inventario_uaeh is not null "
            "        and numero_inventario_uaeh = %s) limit 1",
            (serie, inv))
    else:
        cur.execute(
            """
            select id from public.existencia
             where articulo_id = %s and almacen_id = %s
               and ubicacion_id is not distinct from %s
               and marca        is not distinct from %s
               and presentacion is not distinct from %s
             limit 1
            """,
            (articulo_id, almacen_id, ubicacion_id, campos.get("marca"),
             campos.get("presentacion")))
    fila = cur.fetchone()
    return fila[0] if fila else None


def escribir_hoja(cur, hoja: Hoja, renglones: tuple[Renglon, ...],
                  perfil_id: uuid.UUID) -> int:
    """Devuelve cuantas existencias NUEVAS se crearon."""
    almacen_id = db.almacenes(cur)[hoja.almacen]
    labs = db.laboratorios(cur)
    carga_id = crear_carga(cur, hoja, almacen_id, perfil_id, len(renglones))
    nuevas = 0

    for renglon in renglones:
        c = renglon.campos
        nombre = c.get("sustancia") or c.get("articulo")
        resolucion = catalogo.resolver(
            cur, nombre=nombre, descripcion=c.get("especificacion"),
            unidad=c["unidad"], clasificacion=_clasificacion(hoja, c),
            familia=c.get("familia"), perfil_id=perfil_id)

        if hoja.nombre == "Reactivos" and resolucion.creado:
            cur.execute(
                """
                insert into public.articulo_reactivo
                  (articulo_id, cas, estado_fisico, color_almacenaje,
                   tiene_hoja_seguridad, caracteristica_toxica,
                   caracteristica_quimica, riesgo_salud, riesgo_inflamabilidad,
                   riesgo_reactividad, peligro_especial,
                   implica_actividad_peligro)
                values (%s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s)
                on conflict (articulo_id) do nothing
                """,
                (resolucion.articulo_id, _cas(nombre), c.get("estado_fisico"),
                 c.get("color"), c.get("hoja_seguridad"),
                 c.get("caracteristica_toxica"), c.get("caracteristica_quimica"),
                 c.get("riesgo_salud"), c.get("riesgo_inflamabilidad"),
                 c.get("riesgo_reactividad"), c.get("peligro_especial"),
                 c.get("implica_peligro")))

        if hoja.nombre == "Materia biológica" and resolucion.creado:
            cur.execute(
                "insert into public.articulo_biologico (articulo_id, origen_especie) "
                "values (%s, %s) on conflict (articulo_id) do nothing",
                (resolucion.articulo_id, c.get("origen_especie")))

        ubicacion_id = upsert_ubicacion(cur, almacen_id, c)
        previa = _existencia_previa(cur, hoja, almacen_id,
                                    resolucion.articulo_id, ubicacion_id, c)
        if previa is not None:
            continue

        lab_id = labs.get((hoja.almacen, clave(c.get("laboratorio") or "")))
        cur.execute(
            """
            insert into public.existencia
              (articulo_id, almacen_id, ubicacion_id, laboratorio_id, carga_id,
               marca, modelo, presentacion, peso_frasco_vacio, peso_total,
               numero_serie, numero_inventario_uaeh, funcionamiento,
               mantenimiento, fecha_chequeo, metodo_conservacion, temperatura,
               fecha_recoleccion, fecha_preparacion, responsable_muestra,
               observaciones)
            values (%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s)
            returning id
            """,
            (resolucion.articulo_id, almacen_id, ubicacion_id, lab_id, carga_id,
             c.get("marca"), c.get("modelo"), c.get("presentacion"),
             c.get("peso_vacio"), c.get("peso_total"), c.get("numero_serie"),
             c.get("numero_inventario"), c.get("funcionamiento"),
             c.get("mantenimiento"), c.get("fecha_chequeo"),
             c.get("metodo_conservacion"), c.get("temperatura"),
             c.get("fecha_recoleccion"), c.get("fecha_preparacion"),
             c.get("responsable_muestra"), c.get("observaciones")))
        existencia_id = cur.fetchone()[0]
        nuevas += 1

        # Punto 5 del contrato: la cantidad entra por movimiento, nunca directo.
        # Si es cero no se inserta: el trigger ya dejo la existencia en agotado.
        if c["cantidad"]:
            cur.execute(
                """
                insert into public.movimiento
                  (existencia_id, almacen_id, tipo, cantidad, cantidad_antes,
                   cantidad_despues, usuario_id, motivo)
                values (%s, %s, 'carga_inicial', %s, 0, 0, %s, %s)
                """,
                (existencia_id, almacen_id, c["cantidad"], perfil_id,
                 f"Carga inicial desde {hoja.archivo}"))

    return nuevas


def _cas(nombre: str) -> str | None:
    """El CAS viene enterrado en la cadena larga: «..., CAS: 5144-89-8».

    Dejarlo dentro del texto tira la deduplicacion entera (comentario de
    catalogo.sql).
    """
    hallado = re.search(r"CAS:\s*([0-9]{2,7}-[0-9]{2}-[0-9])", nombre or "")
    return hallado.group(1) if hallado else None
```

Agregar `import re` al bloque de imports de `destino.py`.

Las fechas de chequeo, recoleccion y preparacion ya vienen en ISO desde el
formato (`2026-05-19`) y pasan tal cual; la unica que hay que convertir es
`Actualizado el`, que es `dd/mm/aaaa`, y de eso se encarga `_fecha()`.

Nota: `cantidad_antes` y `cantidad_despues` van en 0 porque el trigger
`movimiento_aplica_saldo` los sobrescribe antes de insertar; son `not null` y
hay que mandar algo.

- [ ] **Step 4: Correr y ver que pasa**

Run: `DATABASE_URL=... python -m pytest etl/tests/test_destino.py -v`
Expected: PASS, 5 pruebas.

- [ ] **Step 5: Commit**

```bash
git add etl/destino.py etl/tests/test_destino.py
git commit -m "feat(etl): escritura de carga, ubicacion, existencia y movimiento"
```

---

### Task 9: La carga completa y sus conteos

**Files:**

- Modify: `etl/cargar.py`
- Create: `etl/tests/test_carga_completa.py`
- Modify: `etl/ejemplos/README.md`

**Interfaces:**

- Consumes: todo lo anterior.
- Produces: `cargar(origen: Path, cadena: str) -> tuple[Informe, int]` (informe
  y existencias nuevas); `main` con `--cargar` funcional.

- [ ] **Step 1: Escribe la prueba que falla**

`etl/tests/test_carga_completa.py`:

```python
"""Los conteos de la §9 del spec. Salen de los fixtures y se pueden recalcular
con etl/ejemplos/generar.py."""

import os
import subprocess
from pathlib import Path

import pytest

from etl import db
from etl.cargar import cargar

pytestmark = pytest.mark.skipif(
    not os.environ.get("DATABASE_URL"),
    reason="necesita Supabase local: supabase start && supabase db reset")

LIMPIOS = Path("etl/ejemplos/datos-almacenes/limpios")
DEFECTOS = Path("etl/ejemplos/datos-almacenes/defectos")


@pytest.fixture
def base_virgen():
    # Estas cuatro pruebas cuentan filas de toda la base, asi que cada una
    # necesita arrancar de cero. Es lento (unos segundos por reset) y esta bien:
    # son cuatro.
    subprocess.run(["supabase", "db", "reset"], check=True)
    yield


def _uno(sql, *args):
    with db.conectar(db.dsn(None)) as con, con.cursor() as c:
        c.execute(sql, args)
        return c.fetchone()[0]


def test_el_juego_limpio_carga_entero(base_virgen):
    informe, nuevas = cargar(LIMPIOS, db.dsn(None))
    assert informe.rechazos == []
    assert nuevas == 164

    assert _uno("select count(*) from public.carga") == 15
    assert _uno("select count(*) from public.existencia") == 164
    assert _uno("select count(*) from public.articulo") == 152
    assert _uno("select count(*) from public.movimiento "
                " where tipo = 'carga_inicial'") == 162
    assert _uno("select count(*) from public.existencia "
                " where estado = 'agotado'") == 2


def test_la_deduplicacion_hizo_su_trabajo(base_virgen):
    cargar(LIMPIOS, db.dsn(None))

    # Acetona: un articulo, tres existencias (N3, N4, LUM).
    assert _uno("select count(*) from public.existencia e "
                "  join public.articulo a on a.id = e.articulo_id "
                " where a.nombre_canonico like 'Acetona,%%'") == 3
    assert _uno("select count(*) from public.articulo "
                " where nombre_canonico like 'Acetona,%%'") == 1

    # Zinc: mismo CAS, DOS articulos.
    assert _uno("select count(*) from public.articulo "
                " where nombre_canonico like 'Zinc en polvo,%%'") == 2

    # Los dos ROTAP: un articulo, dos existencias.
    assert _uno("select count(*) from public.existencia e "
                "  join public.articulo a on a.id = e.articulo_id "
                " where a.nombre_canonico = 'Analizador granulométrico'") == 2


def test_correr_la_carga_dos_veces_no_duplica(base_virgen):
    cargar(LIMPIOS, db.dsn(None))
    _, segunda = cargar(LIMPIOS, db.dsn(None))
    assert segunda == 0
    assert _uno("select count(*) from public.existencia") == 164


def test_el_juego_con_defectos_no_deja_una_sola_fila(base_virgen):
    informe, nuevas = cargar(DEFECTOS, db.dsn(None))
    assert informe.rechazos
    # Las hojas sin rechazo si entran; las rechazadas, ni una fila.
    rechazadas = informe.archivos_rechazados
    for archivo in rechazadas:
        assert _uno("select count(*) from public.carga where archivo = %s",
                    archivo) == 0
```

- [ ] **Step 2: Correr y ver que falla**

Run: `DATABASE_URL=... python -m pytest etl/tests/test_carga_completa.py -v`
Expected: FAIL — `ImportError: cannot import name 'cargar' from 'etl.cargar'`

- [ ] **Step 3: Implementar `cargar()` y conectar `--cargar`**

En `etl/cargar.py`, agregar los imports y la funcion:

```python
from etl import db, destino


def cargar(origen: Path, cadena: str) -> tuple[Informe, int]:
    """Una transaccion por archivo: la hoja entra entera o no entra."""
    informe, vistos, nuevas = Informe(), Vistos(), 0
    with db.conectar(cadena) as con:
        with con.cursor() as cur:
            catalogos = db.catalogos(cur)
            perfil = db.perfil_de_carga(cur)

        for ruta in archivos(origen):
            hoja = leer(ruta)
            validos = validar(hoja, informe, catalogos, vistos)
            if validos is None:
                continue
            try:
                with con.cursor() as cur:
                    nuevas += destino.escribir_hoja(cur, hoja, validos, perfil)
                con.commit()
            except Exception as error:      # noqa: BLE001 — se anota y se sigue
                con.rollback()
                informe.anotar(Problema(
                    archivo=hoja.archivo, hoja=hoja.nombre, fila=None,
                    columna="", regla="Error de la base", valor="",
                    accion="rechazo", detalle=str(error).strip()))
    return informe, nuevas
```

Agregar `from etl.review.informe import Informe, Problema` al bloque de imports.

Y en `main()`, reemplazar el bloque final por:

```python
    catalogos = Catalogos()
    if args.cargar:
        cadena = db.dsn(args.dsn)
        informe, nuevas = cargar(origen, cadena)
        print(f"  {nuevas} existencias nuevas")
    else:
        informe, renglones = simular(origen, catalogos)
        total = sum(len(r) for r in renglones.values())
        print(f"  {len(renglones)} hojas listas, {total} renglones")

    ruta = REVIEW / f"{dt.date.today():%Y-%m-%d}-{origen.name}.csv"
    informe.a_csv(ruta)
    print(f"  {len(informe.rechazos)} rechazos, "
          f"{len(informe.normalizados)} normalizados → {ruta}")

    if not args.cargar:
        print("  simulacro: no se escribió nada. Agrega --cargar.")
    return 1 if informe.rechazos else 0
```

- [ ] **Step 4: Correr todas las pruebas**

```bash
supabase start
supabase db reset
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres \
  python -m pytest etl/tests -v
```

Expected: PASS, todas.

- [ ] **Step 5: Correr la carga de verdad**

```bash
supabase db reset
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres \
  python -m etl.cargar --juego limpios --cargar
```

Expected: `164 existencias nuevas`, `0 rechazos`, codigo 0.

```bash
DATABASE_URL=... python -m etl.cargar --juego defectos --cargar
```

Expected: codigo 1, y el CSV con los problemas.

- [ ] **Step 6: Documentar y commit**

En `etl/ejemplos/README.md`, agregar al final:

```markdown
## Cargarlos

```bash
supabase start && supabase db reset
export DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres

python -m etl.cargar --juego limpios              # simulacro
python -m etl.cargar --juego limpios --cargar     # carga de verdad
python -m etl.cargar --juego defectos             # el informe con los 22
```

El diseño del cargador está en
`docs/specs/2026-08-20-etl-carga-formato-unificado-design.md`.
```

```bash
git add etl/cargar.py etl/tests/test_carga_completa.py etl/ejemplos/README.md
git commit -m "feat(etl): carga completa con transaccion por archivo"
```

---

## Cierre

Antes de dar el trabajo por terminado:

```bash
python -m pytest etl/tests -v      # todas, con DATABASE_URL puesto
python etl/ejemplos/generar.py --verificar
pnpm typecheck && pnpm lint
supabase test db
```

Lo de `pnpm` y `supabase test db` no deberia haber cambiado —este trabajo no
toca `src/` ni las migraciones— pero se corren igual: `seed.sql` cambio, y es lo
que usan las pruebas de RLS.

## Lo que este plan deja pendiente a proposito

- **El valor `No funciona`** se rechaza. Decidir si entra al enum
  `funcionamiento_equipo` por migracion o se mapea a `presenta_fallas` mas
  `estado = 'baja'` es una conversacion aparte.
- **Un renglon de ejemplo que nombrara bien su almacen** pasaria: los tres de
  la plantilla se atajan porque declaran N3 y LUM-2 dentro de un archivo de N4.
- **Una columna de mas no se detecta.** El lector solo mira las letras que
  `CAMPOS` conoce, asi que la «Existencia Ene-Jun 2026» del defecto 13 se ignora
  en silencio en vez de avisar. Ignorarla es el comportamiento correcto; avisar
  pide comparar la fila de encabezado contra la esperada, y eso es otra tarea.
- **La promocion al remoto** —dar de alta `carga@uaeh.local` en el dashboard de
  Auth y correr con su DSN— va despues de que esto salga verde en local.
